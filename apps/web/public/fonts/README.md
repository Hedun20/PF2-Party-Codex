Put fantasy font files here to enable the dark-elven typography layer.

Expected local filenames:

- `BilboHand.woff2` or `BilboHand.ttf`
- `ElvenCommonSpeak.woff2` or `ElvenCommonSpeak.ttf`
- `Iokharic.woff2` or `Iokharic.ttf`

The app falls back to Georgia if these files are not present.
